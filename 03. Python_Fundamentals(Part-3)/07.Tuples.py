# A tuple is an immutable (cannot be changed) collection.
# You cannot add, remove, or modify items once created.
# Tuple is faster than list  because tuple does not change.
# Tuples take less space in memory → better performance.

tuple = (1, 2, 3, 4, 5, 0.78, "syal")
print(tuple)
print(type(tuple))
print(len(tuple))
print(tuple[2])